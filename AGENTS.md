# Codex 协作规则

1. 每次只处理用户明确范围内的任务。
2. 修改前读取相关文件，不反复遍历无关目录。
3. 不擅自改变四项核心功能：客户截图分析、针对性开发信生成、开发信编辑与一键复制、历史任务管理。
4. 不删除已有功能，除非任务明确要求。
5. 不把 API 密钥写入代码。
6. `.env` 不能提交到 Git；只提交不含秘密的 `.env.example`。
7. 数据库修改以后必须使用 migration。
8. AI 输出以后必须进行结构校验。
9. 演示数据和真实数据必须明确区分。
10. 每次较大修改记录到 `prompts/`。
11. 完成任务后运行 Lint、类型检查和相关测试。
12. Git 操作只处理当前项目目录，不修改同一仓库的其他项目。
